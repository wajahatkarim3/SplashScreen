<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wajahatkarim.splashscreen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CoroutinesSplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TimerSplashActivity">
        </activity>
        <activity android:name=".CommonSplashActivity"></activity>
        <activity
            android:name=".SplashLogicActivity"
            android:theme="@style/AppTheme.Splash" />
        <activity
            android:name=".LoginActivity"
            android:label="Login" />
        <activity
            android:name=".SplashThemeActivity"
            android:theme="@style/AppTheme.Splash" />
        <activity
            android:name=".HomeActivity"
            android:label="Home" />
    </application>

</manifest>